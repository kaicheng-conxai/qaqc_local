# The API must be reachable through the local kubectl port-forward.
QAQC_API_BASE_URL=http://127.0.0.1:18000

# Raw decoded key from the protected deployment secret. Never commit it.
QAQC_INTERNAL_API_KEY=
QAQC_API_KEY_HEADER=X-API-Key-s2s
